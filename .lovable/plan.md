## 1. Шапка (`SiteHeader.tsx`)
Удалить пункты «Амплитуда» и «Отзыв». Оставить только:
- Метод → `#method`
- Исследования → `#research`

CTA «Записаться» → `#lead-top` без изменений.

## 2. График хаос → амплитуда (`WaveTransition.tsx`)
Перерисовать обе половины:

**Левая часть (хаос).** Сейчас — мягкие синусы. Сделать резко-ломаной полилинией:
- крупный шаг по X (≈40–60px), Y случайно прыгает в диапазоне ±70…90 от середины
- острые углы (`strokeLinejoin="miter"`), без сглаживания
- 3–4 наложенных слоя с разным сидом и смещением, разные толщины — ощущение турбулентности и шума
- сохранить градиент `painGrad` и анимацию проявления

**Правая часть (амплитуда).** Сейчас — синус. Заменить на стабильный растущий тренд слева-направо снизу-вверх:
- начинается у `y ≈ mid + 50` (низ), заканчивается у `y ≈ mid − 80` (верх)
- почти прямая линия с очень малой синусоидной рябью (амплитуда 4–6px), чтобы читалось как «живой, но стабильный сигнал»
- сохранить градиент `solGrad`, glow, частицы — частицы расставить вдоль нового восходящего пути
- echo-линии тоже по новому пути

## 3. Верхняя форма (`LeadFormSection.tsx`, `variant="top"`)
Убрать тройное повторение слова «консультация». Для `variant="top"`:
- Eyebrow: `Бесплатная консультация` (без изменений)
- Заголовок: `Давайте обсудим ваши зоны роста`
- Подзаголовок: `Бесплатная 45-минутная сессия с представителем Indigo Lab.`
- Текст кнопки: `Записаться` (вместо «Записаться на консультацию»), состояние загрузки — `Отправляем…`

Нижнюю форму (`variant="bottom"`) не трогаем.

## Затрагиваемые файлы
- `src/components/landing/SiteHeader.tsx`
- `src/components/landing/WaveTransition.tsx`
- `src/components/landing/LeadFormSection.tsx`
