## Мелкие правки

1. **Чекбокс согласия в лид-форме** — `LeadFormSection.tsx`: `useState(false)` → `useState(true)`. Применяется к обеим формам (top и bottom), т.к. компонент один.

2. **Кнопка в калькуляторе** — `HiddenProfitCalculator.tsx`: текст `Получить разбор` → `Записаться на сессию`. Действие (скролл к `#lead-bottom`) и иконка стрелки остаются.

3. **FAQ** — `FaqSection.tsx`: удалить пункт `А если программа не подойдёт?` (5-й в массиве `faqs`). Остаются 5 вопросов: длительность, цена, формат, сроки, кто ведёт.

Скажите «поехали» — реализую за один проход.
